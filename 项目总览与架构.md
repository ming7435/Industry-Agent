# Industry-Agent：项目总览与架构

## 项目定位

面向工业设备运维和生产零件质检的 Multi-Agent 平台，核心链路是：

```text
模拟工厂 → Monitor → 异常规则 → Agent Orchestrator
→ Diagnosis → Knowledge / CAD → Maintenance → WorkOrder
→ 维修反馈 / 验证 → Memory / Report
```

Quality Agent 独立负责生产零件质检，不负责设备维修验收。

## 服务

| 服务            |   端口 | 作用                   |
| ------------- | ---: | -------------------- |
| Monitor       | 8001 | 采集设备快照、确认异常、提供工作台    |
| Agent Service | 8010 | 编排 Agent、接收用户问题和异常事件 |
| CAD Service   | 8011 | 查询图纸、BOM、部件和装配关系     |
| RAG Service   | 8020 | 解析、检索、融合、重排和证据组织     |
| 模拟工厂          | 4529 | 提供设备状态、历史和日志         |

## 顶层编排

源码：`services/agent-service/app/graph/workflow.py`

`AgentOrchestrator` 注册节点：`route`、`diagnosis`、`knowledge`、`cad`、`maintenance`、`workorder`、`quality`、`memory`、`report` 以及工单动作/查询节点。

用户入口和自动异常入口共用同一张图：

```text
用户问题：route → 目标 Agent → report / END
自动异常：route → diagnosis → knowledge → cad → maintenance → workorder → END
```

自动异常在创建工单后结束；维修关闭、经验学习和报告由后续事件或动作入口推进。

## AgentState

源码：`services/agent-service/app/graph/state.py`

共享状态字段分为：

- 标识：`task_id`、`trace_id`
- 输入：`entry`、`user_text`、`event`、`context`
- Agent 结果：`diagnosis`、`knowledge`、`cad`、`maintenance_plan`、`quality`
- 业务结果：`workorder_result`、`report`、`memory_result`
- 运维：`status`、`errors`、`trace`

节点通过这些字段交换结构化数据，而不是直接互相修改内部对象。

## 分层边界

```text
API / Monitor
  → Orchestrator / Agent
  → Service（确定性业务）
  → Tool Registry（统一工具入口）
  → MCP / 外部系统 / 数据库 / RAG / CAD
```

Agent 负责理解和编排，Service 负责业务规则，Tool 负责外部能力调用。

## Agent Harness 与 Agent Loop

`AgentHarness` 是单个 Agent 的统一运行边界：负责超时、重试、执行编号和 Trace。Agent 内部的 `Agent Loop` 则负责 Observe → Reason → Tool → Observe → Validate 的证据驱动循环，两者不是同一个概念。
