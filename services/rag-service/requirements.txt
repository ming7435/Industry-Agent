# RAG service dependencies (Python 3.11+).
# Both halves of the service share this file: the offline ingestion pipeline and
# the online retrieval/generation API. Nothing here is test-only.

# --- web layer (online) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- retrieval stores ---
pymilvus>=2.5.0
whoosh==2.7.4
jieba>=0.42.1

# --- offline document parsing ---
PyMuPDF>=1.24,<2
python-docx>=1.1,<2
openpyxl>=3.1,<4
Pillow>=10.0,<12
ezdxf>=1.3,<2

# --- remote embedding / reranking ---
# BGE-M3 embeddings and bge-reranker-v2-m3 reranking are called through
# SiliconFlow's HTTP API; no local model runtime is required.

# --- generation (online) ---
# Provider SDKs are intentionally not installed here. RAG calls Model Service
# over HTTP; provider credentials and SDKs belong exclusively to Model Service.

# --- offline metadata store ---
pymysql>=1.1,<2

# --- offline original-file storage (MinIO/S3) ---
minio>=7.2,<8

# --- configuration / observability ---
python-dotenv>=1.0,<2
loguru>=0.7.2
