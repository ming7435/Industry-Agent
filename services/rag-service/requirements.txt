# RAG service dependencies (Python 3.11+).
# Both halves of the service share this file: the offline ingestion pipeline and
# the online retrieval/generation API. Nothing here is test-only.

# --- web layer (online) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- retrieval stores ---
pymilvus>=2.5.0
whoosh==2.7.4
jieba>=0.42.1

# --- offline document parsing ---
PyMuPDF>=1.24,<2
python-docx>=1.1,<2
openpyxl>=3.1,<4
ezdxf>=1.3,<2

# --- models (bge-m3 embedder + bge-reranker-v2-m3) ---
FlagEmbedding>=1.3.0
sentence-transformers>=3.0,<4
numpy>=1.26.0
# torch is required by FlagEmbedding / sentence-transformers; install the CUDA
# build matching the host driver when the reranker is meant to run on GPU
# (settings.reranker_device).
torch>=2.1.0

# --- generation (online) ---
openai>=1.40.0

# --- offline metadata store ---
pymysql>=1.1,<2

# --- configuration / observability ---
python-dotenv>=1.0,<2
loguru>=0.7.2
