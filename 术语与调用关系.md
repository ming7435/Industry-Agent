# 术语与调用关系

## 核心词汇

| 词 | 在项目中的含义 |
|---|---|
| Agent | 有输入、状态、工具和输出的业务智能体 |
| Orchestrator | 负责把多个 Agent 编排成流程的顶层对象 |
| Graph | LangGraph 中由节点和边组成的可执行流程 |
| State | 节点之间传递的数据容器 |
| Tool | Agent 可调用的确定性能力 |
| Tool Registry | 工具注册、查找、执行和记录的统一入口 |
| Skill | 某类任务允许使用的工具组合与约束 |
| A2A | Agent-to-Agent，Agent 之间的请求与返回 |
| Observation | 一次 Tool 或 A2A 调用后的观察结果 |
| Evidence | 支持结论的证据文本、指标或引用 |
| Validator | 对模型输出进行结构和业务校验的组件 |
| Fallback | 模型不可用、证据不足或超限时的保守路径 |
| RRF | Reciprocal Rank Fusion，多路检索按排名融合 |

## Agent → Tool

```text
Agent
→ Tool Guard（Diagnosis 中）
→ ToolRegistry.execute(name, arguments)
→ 具体 Tool
→ RAG / CAD / 工厂 API / 数据库
→ 结构化 result
→ AgentState 或 Observation
```

## Agent → Agent

```text
OrchestratorNodes
→ Agent 方法
→ 读取共享 AgentState
→ 执行自身 graph / service / tool
→ 返回字段更新
→ 下一个节点读取更新后的 AgentState
```

Diagnosis 的特殊路径：

```text
Diagnosis
→ knowledge_provider.request_knowledge(...)
→ Knowledge Agent
→ search_knowledge Tool
→ KnowledgeResult
→ Diagnosis Observation / Evidence
```

## 追踪任何调用的办法

1. 从 Agent 方法找到写入的状态字段。
2. 找 `requests`、`provider` 或 `tools.execute` 调用。
3. 根据工具名称找到 Registry 注册位置。
4. 找具体 Tool 的参数和返回值。
5. 继续追踪它访问的 API、数据库或索引。
6. 回到 Agent，确认结果写入哪个状态字段。
7. 确认下一个 Agent 从哪里读取该字段。
