# Industry-Agent 源码详细讲解

## 1. Diagnosis Agent

源码：`services/agent-service/app/agents/diagnosis/graph.py`

### 节点执行顺序

```text
initialize → load_skill → reason → tool_guard → act → observe
→ loop_guard → reason（循环）→ validate → final / fallback
```

### `initialize_diagnosis_state`

- 从 `state["agent"]` 取当前 Agent。
- 从 `state["agent_state"]` 取 Diagnosis 内部状态。
- 用 `build_diagnosis_messages(event)` 把异常事件转换为模型消息。
- 设置 `runtime.status = RUNNING`。
- 如果 `agent.client.available` 为假，写入 `llm_unavailable`，转 `fallback`。

### `load_diagnosis_skill`

- `tool_policy.select_skill(event)` 根据事件选择 Skill。
- `get_skill_registry().get("diagnosis", name)` 读取 Skill 定义。
- 将 Skill 合并出的工具写入 `runtime.allowed_tools`。
- 后续模型只能在这个白名单内请求工具。

### `request_diagnosis_reasoning`

- 检查 `step_count` 是否超过 `max_steps`。
- 调用 `agent.client.chat(messages, tools, tool_choice)`。
- 从响应中解析 `assistant` 和 `tool_calls`。
- 有报警码且尚未查询定义时，优先调用 `get_alarm_definition`。
- 没有工具调用但存在 `knowledge_provider` 时，通过 `agent.request_knowledge` 发起 A2A 知识请求。
- 有工具调用转 `tool_guard`，没有工具调用转 `validate`。

### `guard_tool_calls`

每个模型工具调用都经过：解析函数名 → 解析 JSON 参数 → 参数标准化 → `tool_policy.guard_tool_call`。

- 允许：写入 `guarded_calls`，转 `act`。
- 拒绝：生成失败结果，写入消息和 `tool_results`，回到 `reason`。

### `execute_tool_calls`

通过：

```python
agent.tools.execute(name, arguments)
```

执行具体 Tool。每次调用记录名称、参数、结果、耗时和错误，随后交给 `observe`。

### `record_tool_observations`

`evidence.observation_from_tool` 把原始 Tool 结果转换为 Observation；再提取 Evidence 和 Evidence Record，并追加到运行时状态与模型消息。

通过 Observation 指纹检测重复结果，供 `loop_guard` 阻止无意义循环。

### `loop_guard`

- `step_count >= max_steps`：超过最大工具调用轮次，转 `fallback`。
- `repeated_observation_count >= 1`：没有新证据，转 `fallback`。
- 否则回到 `reason`。

### `validate_diagnosis_candidate`

解析模型 JSON，调用 `agent._validate_candidate` 检查格式、字段、证据和置信度。

- 通过：`final`。
- 不通过但仍有轮次：把错误写入系统消息，回到 `reason`。
- 不通过且达到上限：`fallback`。

## 2. Agent 与 Agent 的实际调用

自动异常在顶层通过共享 `AgentState` 串联：

```text
Diagnosis 输出 state[diagnosis]
→ Knowledge 读取 diagnosis，写 state[knowledge]
→ CAD 读取 diagnosis/context，写 state[cad]
→ Maintenance 读取 diagnosis/knowledge/cad，写 state[maintenance_plan]
→ WorkOrder 读取 maintenance_plan，写 state[workorder_result]
```

Diagnosis 还可以通过 `knowledge_provider` 直接发起 Knowledge A2A 请求，把返回值写入自己的 Observation，而不是等待外层重复检索。

## 3. Knowledge Agent

源码：`services/agent-service/app/agents/knowledge/agent.py`

`search_knowledge` 的实际步骤：

1. 清理 query。
2. `_classify_query` 分类为 `alarm`、`manual`、`sop`、`case`、`engineering` 或 `hybrid`。
3. `_build_filters` 从查询中提取报警码或知识类型。
4. 调用 `tools.execute("search_knowledge", payload)`。
5. `_deduplicate_documents` 按 document_id 保留最高分文档。
6. 生成 evidence、possible_causes、recommended_checks。
7. 计算 confidence 和 degraded 状态。
8. 返回 `KnowledgeResult`。

## 4. WorkOrder Service

源码：`services/agent-service/app/workorder/service.py`

`create_from_plan`：维修计划 → 校验 → 提取 `device_id`、`alarm_code`、`repair_steps`、`target_part`、`drawing_context` → 保存诊断/计划快照 → `ToolRegistry.execute("create_workorder", payload)`。

源码：`services/agent-service/app/workorder/validator.py`

- 允许状态：`open`、`in_progress`、`completed`、`closed`、`rejected`、`timeout`。
- 维修经验只有在 `status == closed` 且反馈有有效文本时才能学习。

## 5. RAG 代码链路

源码：`services/rag-service/app/ingestion/parser.py`

`parse_document` 按扩展名选择 PDF、Markdown、DOCX、CSV、XLSX、图片和 CAD 解析器，统一输出 `StructuredDocument`，并添加 document_id、content_hash、file_size。

源码：`services/rag-service/app/retrieval.py`

`Hit` 统一表示一条检索结果：`chunk_id`、`text`、`score`、`source`、`metadata`、`rank`。

源码：`services/rag-service/app/fusion/rrf.py`

BM25 和向量分数不可直接相加，因此使用：

```text
RRF score = Σ 1 / (k + route_rank)
```

同一路由重复 chunk 只取最佳排名，多路由同时命中的 chunk 累积得分。

源码：`services/rag-service/app/reranker/pipeline.py`

重排器首次使用时懒加载，并通过锁保证单例；加载失败时返回 `None`，由服务按降级策略继续运行。

## AgentContainer：运行时装配根

源码：`services/agent-service/app/runtime/container.py`

`AgentContainer` 不是业务 Agent，而是依赖装配对象。它把同一套资源注入整个 Orchestrator：

```text
Settings → ToolRegistry → TraceRecorder → A2AClient
→ Memory Stores → WorkOrderService / ClosureService
→ ExperienceLearningModule → A2ARequests
→ 9 个 Agent Registry → AgentHarness → A2AEndpoints
→ RuntimeOperations / NodeTrace
```

所有 Agent 共用一个 `TraceRecorder`；ToolRegistry 的 trace 也指向它，因此 Agent、Tool、A2A 和节点能落到同一条调用链。

启动时 `get_skill_registry().validate_tools(registry.mcp.handlers)` 会检查 Skill 声明的工具是否真实注册。

## A2A 协议

源码：`services/agent-service/app/a2a/models.py`、`client.py`、`registry.py`

`A2ARequest` 的核心字段是 `request_id`、`message_id`、`task_id`、`trace_id`、`from_agent`、`to_agent`、`action` 和 `payload`。模型校验器会自动同步 `source_agent`、`target_agent` 和缺省的 `message_id`。

`A2AResponse.reply_to` 必须指向原始 Request 的 `message_id`。`A2AClient._validate_response` 还会检查 request_id、task_id、trace_id 和 Agent 方向，防止串任务、错回复和跨 Agent 污染。

`ALLOWED_A2A_ROUTES` 限制协作路径。例如 Diagnosis 可以调用 Knowledge、CAD、Memory、Maintenance；Memory 不能主动调用任何 Agent；Report 只能调用 Memory。

## Loop、Harness、Container 的区别

```text
Agent Loop  = Agent 内部决定是否继续取证
AgentHarness = Agent 一次运行的超时、重试和 Trace 生命周期
AgentContainer = Agent 运行依赖的统一装配
```

## 6. Agent Harness

源码：`services/agent-service/app/harness/runtime.py`

`AgentHarness` 是 Agent 的运行时外壳，不负责业务推理。它统一处理：

- `timeout_seconds`：单次 Agent 执行的超时时间，默认 45 秒。
- `max_retries`：失败后的最大重试次数，默认 1 次。
- `agent_run_id`：每次尝试单独生成的执行编号。
- `TraceRecorder`：记录 started、completed、timeout、error。

执行逻辑：

```text
execute_agent
→ 判断 Agent 名称和重试策略
→ 为每次尝试创建单线程 ThreadPoolExecutor
→ executor.submit(agent.run, abnormal_event)
→ future.result(timeout=...)
→ 成功返回 / 超时记录 / 异常记录
→ 需要时重试
→ 最终抛出 AgentExecutionError
```

WorkOrder、Memory、Quality 被强制设置为 `max_retries = 0`。原因是 Python 线程超时后不能被强制停止；如果这些有副作用的 Agent 被自动重试，可能出现并发重复写入。因此副作用 Agent 必须依赖幂等边界，而不是依赖 Harness 重试。

## 7. TraceRecorder

源码：`services/agent-service/app/harness/trace.py`

`TraceRecorder` 使用带锁的定长 `deque` 保存记录：

- `record()`：追加 UTC 时间戳和调用负载。
- `list(trace_id, task_id, limit)`：按链路或任务过滤。
- `clear()`：清空内存记录。

它是轻量级内存实现，源码注释说明未来可以替换成 OpenTelemetry。当前 Trace 不是永久事件存储，进程重启后记录会消失。

## 8. Agent Loop

项目没有单独名为 `AgentLoop` 的类；Agent Loop 主要实现于 Diagnosis Graph：

```text
reason
→ tool_guard
→ act
→ observe
→ loop_guard
→ reason
```

循环每轮都会增加 `step_count`，并将 Tool 结果转换为 Observation 和 Evidence。`loop_guard` 通过两个事实停止循环：

1. 达到 `max_steps`。
2. 新一轮 Observation 与上一轮重复。

因此这里是 Evidence-driven Loop（证据驱动循环），不是无限对话循环。模型不能仅凭“我还想继续”无限调用工具，必须产生新证据并通过 Validator。

## 9. Harness 与 Loop 的区别

| 对象 | 负责什么 | 作用范围 |
|---|---|---|
| AgentHarness | 超时、重试、执行编号、外部 Trace | Agent 一次完整运行 |
| Agent Loop | 推理、工具调用、观察、证据、校验 | Agent 内部多轮执行 |
| Orchestrator | 多 Agent 节点顺序和状态传递 | 整个业务流程 |

## Runtime Operations、幂等与持久化

源码：`services/agent-service/app/runtime/operations.py`

`RuntimeOperations` 是 API/事件入口使用的业务操作层，和 LangGraph 节点使用同一套 `A2ARequests`。工单动作仍发送给 WorkOrder Agent，经验动作仍发送给 Memory Agent，质检结果再由 ClosureService 持久化。

关闭工单的真实链路：

```text
execute_workorder(close)
→ WorkOrder Agent 关闭
→ status == closed 且反馈有效
→ learning_key = workorder:<workorder_id>
→ Memory Agent action=learn
→ RAG 写入经验
→ Report Harness 生成 full_case_report
```

源码：`services/agent-service/app/runtime/event_store.py`、`durable_store.py`

`EventResultStore` 以非空 `event_id` 作为幂等键，先查内存 `OrderedDict`；配置 `EVENT_STORE_PATH` 后使用 SQLite `DurableJsonStore` 跨进程保存。`get_or_create` 通过 `BEGIN IMMEDIATE` 串行化并发生产者，保证同一事件只有一个真正执行。

## Monitor 异常生命周期

源码：`services/agent-service/app/monitor/monitor.py`

`DeviceMonitor.observe(sample)` 的顺序是：

```text
按 device_id 取得 _DeviceState
→ 校验时间戳
→ 保存采样
→ 检测异常
→ 清理过期状态
→ 重置已恢复的连续异常
→ 更新 _AnomalyState
→ 准备 incident
→ 分类设备状态
→ 判断是否满足触发条件
→ 回调 on_trigger
```

`_DeviceState` 保存单台设备的采样、异常状态、incident_id 和报警码集合。`_AnomalyState` 保存首次出现、最近出现、持续起点、次数、严重度、事件版本和上次诊断等级。

`_build_trigger_if_ready` 的门槛是：`CRITICAL` 立即触发；`TREND`/`MULTI_METRIC` 确认后触发；`COUNT` 达到时间窗口次数；`DURATION` 达到持续时间。同一事件只有首次确认或等级升级才再次触发 Diagnosis。

触发后生成 `AbnormalEvent` 和 `DiagnosisTrigger`，并通过 `event_revision` 与 `task_id=TASK-<event>-R<revision>` 区分同一故障生命周期中的多次诊断。

## API 与共享数据契约

源码：`services/agent-service/app/api/server.py`、`app/contracts.py`

API 层使用 Pydantic Request 模型做边界校验，再调用 `AgentOrchestrator` 或 `RuntimeOperations`；它不直接访问 Agent 内部实现。

重要入口：

```text
POST /api/v1/agent/question → run_user
POST /api/v1/agent/event → EventResultStore → run_abnormal_event
GET  /api/v1/rag/search → ToolRegistry.search_knowledge
POST /api/workorders → RuntimeOperations.execute_workorder
POST /api/v1/workorders/{id}/complete → execute_workorder(mark_repair_completed)
POST /api/v1/quality/checks → ClosureService
GET  /api/v1/trace → TraceRecorder.list
```

`app/contracts.py` 是 Agent 间的共享数据契约，关键模型包括 `RouteResult`、`KnowledgeResult`、`CADResult`、`MaintenancePlan`、`WorkOrder`、`QualityResult`、`ReportResult` 和 `ExperienceResult`。这些模型用 Literal、Field 范围和默认结构限制跨 Agent 数据形状。

特别要区分：

```text
pipeline.status = waiting_repair
workorder.status = open
```

前者是整个异常处理流程的状态，后者是工单实体自身的生命周期状态。

## Quality Closure

源码：`services/agent-service/app/closure/service.py`

`ClosureService` 管理生产零件质检记录、申诉、整改任务和审计日志。它通过 `build_closure_store()` 选择 MySQL 或内存后端，内存回退用于本地演示。

质检主链路：

```text
record_part_quality
→ create_quality_check
→ 生成 QC-<id>
→ 保存检查结果
→ 写 audit log
```

申诉会把质检记录状态改为 `appealed`，并追加 `appeal_id`；整改任务独立拥有 `open` / `completed` 状态。每个状态变化都通过 `_audit` 记录操作人、对象和变更内容。

Quality Agent 负责判断零件质量，ClosureService 负责持久化和审计，这就是推理层与确定性业务层的边界。

## RAG 在线 Pipeline

源码：`services/rag-service/app/api/pipeline.py`

`SearchPipeline.search()` 的真实阶段是：

```text
retrieve → fuse → rerank → evidence → generate
```

BM25 和 dense 两条检索腿通过 `asyncio.gather(..., return_exceptions=True)` 并发执行；一条失败不会取消另一条。同步的 Whoosh、Milvus 和 reranker 通过 `asyncio.to_thread` 放入线程，并使用 `wait_for` 限制预算。

每个请求都有 `_Progress`，保存两路 hits、融合结果、最终结果、EvidenceBundle、答案、各阶段耗时和降级原因。因此即使全局请求超时，也能返回已经拿到的部分结果。

降级顺序明确：

- dense 失败：保留 BM25
- BM25 失败：保留 dense
- 两路失败：`all_retrievers_failed`
- reranker 失败：使用 RRF 顺序
- LLM 失败：返回证据但答案为空
- 总预算超时：返回已有部分结果

## Evidence Layer

源码：`services/rag-service/app/evidence/builder.py`

Evidence 层不重新检索，而是把最终 Hit 变成可引用证据：

1. 按 corpus 分组，优先顺序是 alarms、cases、manuals、sop。
2. 按 score 降序排列。
3. 使用字符 3-gram Jaccard，相似度达到 0.85 的 chunk 视为重复。
4. 生成 `[1]`、`[2]` 格式 citation_id。
5. 统一抽取 source、device_model、error_code 和 text。

这一步把“检索排序”转换为“技术人员可读、可引用的证据包”。

## Embedding 入口过滤

源码：`services/rag-service/app/embedding/pipeline.py`

`should_embed_chunk` 会拒绝空文本、过短文本、低质量文本，以及视觉识别状态为 pending、failed、disabled 的 chunk。`iter_embed_chunks` 按 batch 流式生成向量，校验向量数量和维度，再生成 `VectorRecord`，避免一次性把所有文档装入内存。

## 10. Skill Registry

源码：`services/agent-service/app/skills/registry.py`

Skill 是 YAML 能力定义，不是一个新的 Agent。`SkillDefinition` 将 YAML 统一成结构化对象，字段包括 `agent`、`name`、`version`、`goal`、`trigger`、`steps`、`tools`、`required_inputs` 和 `optional_inputs`。

`SkillRegistry.list(agent)` 扫描 `app/skills/{agent}/*.yaml` 并缓存结果；`get` 按名称查找；`select` 优先使用显式名称，否则依据 trigger 和上下文匹配，最后回退到 default Skill。

`merge_tools` 和 `merge_steps` 会把多个 Skill 的工具/步骤去重合并。`validate_tools` 在启动时检查重复 Skill 名称以及 Skill 引用的不存在工具。

```text
YAML Skill → Registry → 工具名校验 → Tool Guard → ToolRegistry
```

## 11. MCP Client

源码：`services/agent-service/app/mcp/client.py`

`McpClient.call(server, operation, arguments)` 有两条路径：

1. 配置 `MCP_<SERVER>_URL` 时，向 `{base_url}/tools/call` 发 JSON 请求。
2. 未配置远程地址时，从本地 `handlers` 字典查找同名处理器。

所以 MCP 是调用边界，不代表一定使用远程部署。当前项目可以用本地 handler 模拟 PLC、MES、CAD；配置 URL 后切换到远程服务。

## 12. ExperienceLearningModule

源码：`services/agent-service/app/memory/service.py`

这是经验学习模块，不是 Memory Agent 本身。它组合 ShortMemoryStore、LongMemoryStore、ExperienceExtractor、ExperienceValidator、ExperienceWriter 和 RAGServiceClient。

`search` 只查询长期记忆；`learn` 先验证工单是否关闭且有有效反馈，再抽取经验，最后由 Writer 同时写入 Memory 和 RAG。拒绝学习时返回 `passed=False`，而不是抛出异常。

```text
closed WorkOrder + valid feedback
→ ExperienceValidator
→ ExperienceExtractor
→ ExperienceWriter
→ Long Memory + RAG
```

## CAD Agent

源码：`services/agent-service/app/agents/cad/agent.py`

CADAgent 的 `run` 先用 `CADQuery.from_payload` 规范化请求，再执行 CAD LangGraph，最终必须从图结果中取到 `CADResult`，否则抛出错误。它不直接拼接图纸数据，查询细节由 graph 节点和 ToolRegistry 完成。

## Maintenance Agent

源码：`services/agent-service/app/agents/maintenance/agent.py`

维修计划生成链路：

```text
DiagnosisView
→ Knowledge（优先 SOP，空结果再扩大搜索）
→ CAD（query_part + query_bom + get_component_location）
→ Memory / Inventory
→ generate_repair_plan
→ pre_checks / repair_steps / post_checks
→ tools / parts / safety / evidence
→ target_part + engineering_context
→ MaintenancePlan
```

`_profile` 根据诊断文本和 CAD 部件把故障归为 thermal、vibration 或 general，进而决定维修目标、预计时长、工具、备件和安全要求。`_normalize_diagnosis` 把不同 Agent 返回的字典统一成 `DiagnosisView`。

## WorkOrder Agent

源码：`services/agent-service/app/agents/workorder/agent.py`

WorkOrderAgent 的源码边界明确：不诊断、不制定维修方案、不做质检，只编排工单生命周期。

派工上下文由多个 Tool 组合：

```text
query_technicians
→ query_technician_skills
→ query_technician_workload
→ query_shift
→ query_team_availability
→ WorkOrderAgentValidator.score_candidate
→ 按分数、工作量、technician_id 排序
```

工单创建的幂等缓存当前是进程内 `_idempotency` 字典；源码明确提示生产多实例应迁移到 Redis/DB 唯一键，否则重启或横向扩容可能重复创建。

## Router Agent

源码：`services/agent-service/app/agents/router/agent.py`

Router 先抽取实体，再计算意图得分。实体包括设备编号、报警码、工单号、零件号、批次号、生产订单号、部件和报告类型。

意图不是简单 first-match，而是关键词加权：例如“创建工单”权重 7，“诊断”权重 5，“BOM”权重 5。最高分唯一时返回目标意图；最高分并列时返回 `need_more_context`，避免在歧义下执行错误业务动作。

`workorder_action` 和 `workorder_query` 最终都映射到 `target_agent=workorder`，但 target_input 中的 `action` 不同。

## Quality Agent

源码：`services/agent-service/app/agents/quality/agent.py`

Quality Agent 的输入是生产零件质检请求，不是维修工单验收。它的 Graph 负责取得零件、尺寸、外观、材料、功能和工艺检查结果，再形成 `QualityResult`。

输出中的 `evidence` 会记录各类检查结果；验证失败时不伪造合格结论，而是返回 `status=review`、`passed=False` 和 `quality_input_missing` 等问题，要求补充零件编号、检测数据或规格。

## Report Agent

源码：`services/agent-service/app/agents/report/agent.py`

Report Agent 只汇总已有记录，不重新诊断，也不新增故障事实。它根据已有 sections 推断报告类型：

```text
diagnosis → diagnosis_report
diagnosis + plan + order → maintenance_report
quality → quality_report
diagnosis + plan + order + feedback/verification → full_case_report
```

`_completeness_findings` 检查报告类型所需字段，`_source_refs` 为诊断、报警、Tool、知识文档、CAD 部件、工单、反馈和质检生成引用关系。缺字段时报告状态是 `incomplete`，而不是假装完成。

## Memory Agent

源码：`services/agent-service/app/agents/memory/agent.py`

Memory Agent 只管理已验证维修经验的检索与沉淀。它把请求规范化为 `MemoryQuery`，搜索参数固定从 device_id、device_model、alarm_code、fault_type、component、part_no、query 和 limit 中取值。

真正的学习动作由 `ExperienceLearningModule` 承担；Memory Agent 是编排入口，模块才负责 Validator、Extractor、Writer 和 RAG 双写。`recent` 读取长期记忆的 recent 接口，不会把短期运行状态误当作长期经验。
